{
  "name": "@volta-oss/social-proof-wall",
  "version": "3.0.0",
  "private": false,
  "type": "module",
  "description": "Collect testimonials, review quotes, press snippets, and before-after proof into reusable web sections.",
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "lint": "npm run typecheck",
    "format:check": "node scripts/check-format.js",
    "test": "npm run build && node --test test/*.test.js",
    "start": "python3 -m http.server 4173",
    "validate": "npm test",
    "certify": "npm run build && node --test test/v3.test.js",
    "test:all": "npm test",
    "clean": "rm -f src/*.js src/*.js.map src/*.d.ts test/*.js test/*.js.map test/*.d.ts",
    "ci": "npm ci && npm run format:check && npm test && npm run test:backend --if-present",
    "typecheck": "tsc -p tsconfig.json --noEmit",
    "docker:build": "docker build -t volta-oss/social-proof-wall:local .",
    "docker:run": "docker compose up --build"
  },
  "keywords": [
    "digital-equity",
    "local-first",
    "marketing-content",
    "oss",
    "release",
    "volta"
  ],
  "license": "MIT",
  "repository": {
    "type": "git",
    "url": "git+https://github.com/volta-npo/social-proof-wall.git"
  },
  "homepage": "https://volta-npo.github.io/social-proof-wall/",
  "bugs": {
    "url": "https://github.com/volta-npo/social-proof-wall/issues"
  },
  "author": "Volta Open Source <volta.npo@gmail.com>",
  "engines": {
    "node": ">=20"
  },
  "devDependencies": {
    "typescript": "^6.0.3",
    "@types/node": "^25.9.1"
  }
}
